# AI 开发规则 (AI Development Rules)

## 一、 工作流程与确认机制
* **先分析，后编码**：涉及可行性评估、方案设计时，**只出方案，严禁写代码**。
* **大改动前置确认**：涉及架构、新模块、数据库变更、重构或修改 $\ge 3$ 个文件时，须先提交方案（目标、思路、影响范围），收到“确认/go”后方可动手。
* **分步节点汇报**：复杂任务按节点（表结构 $\rightarrow$ 接口设计 $\rightarrow$ 核心算法 $\rightarrow$ 测试）分步暂停。每步汇报：① 本步成果 ② 关键摘要 ③ 等待确认。
* **小改动直接执行**：单文件、逻辑清晰的 Bug 修复或微调，可直接修改，无需前置方案。
* **严禁盲目猜测**：需求或业务逻辑不明时，必须主动提问，不知道就直说。

## 二、 代码修改与重构原则
* **修改前三步走**：① 先检索相似功能。② 优先修改已有实现，而非新增。③ 发现重复逻辑须建议合并。
* **三不红线禁令**：未经允许，**严禁新增第三方依赖、严禁改动目录结构、严禁改变既有命名风格**。
* **最小修改与克制**：只改相关代码，严禁动历史逻辑。函数职责单一，严禁为未来可能的需求提前抽象。
* **防臃肿强控指标**：单函数 $\le 50$ 行，单文件 $\le 300$ 行。**超标必须立即停止新功能，先提模块化拆分方案**。
* **完成即时重构**：每完成一个功能/Bug修复，**进入下个需求前**，必须删除重复/未使用代码，简化调用逻辑。

## 三、 输出与答复规范
* **增量输出**：默认只输出修改部分的 Diff 或核心代码片段，禁止整文件复制。
* **答复详略分级**：局部微调/语法纠错 $\rightarrow$ 极简回答；方案/大改动 $\rightarrow$ 结构化严谨展开。
* **默认中文沟通**：方案、说明及代码注释默认使用中文；代码标识符遵循项目既有风格。

## 四、 触发执行命令
每次收到任务后，必须先在回复开头进行【三秒自审】（可极其简短）：
1. 评估此修改是否会打破单一职责或突破 50行/300行 限制？
2. 明确当前应该【只出方案】、【等待确认】还是【直接修改】？