# 核心原则
1. 绝对不要擅自修改、删除任何未在当前对话中明确指定的代码文件。
2. 在修改代码前，如果涉及架构变更或核心逻辑，必须先列出方案等待确认，不要直接生成大段代码。
3. **大改动必须先确认再动手**：新增功能模块、新接口/新服务、跨多文件重构、Agent 化等范围较大的工作，必须先给出方案摘要（目标、接口、改动文件、步骤），等用户明确回复「确认 / 可以 / go」后再写代码；用户只问「能不能做 / 怎么做 / 先分析」时，**只分析不写代码**。
4. 严格禁止引入任何未包含在当前项目 package.json/requirements.txt 中的第三方依赖。
5. 复杂任务分步推进：每到一个关键节点（方案、表结构、依赖、造数、核心逻辑、接口设计、批量测试等）必须暂停，汇报并询问用户是否正确，确认后再做下一步；禁止一次做完整个功能。

# 代码风格与行为
6. 保持代码精简，仅输出修改的部分（Diff），不要每次都完整打印整个文件。
7. 如果不知道或上下文不足，直接说“不知道”，严禁瞎编（Holograph/Hallucination）。
8. 严格遵守现有项目的代码规范和变量命名风格。


# 轻量问答
- 用户只圈选少量代码并问「对不对 / 笔误 / 语法」时：仅根据附件回答，禁止读取整个文件，除非用户明确要求看上下文。
- 能一句话答完的不要展开解释、不要复述整段逻辑。