# AI 开发规则 (AI Development Rules)
## 一、工作流程与确认机制
1. **先分析，后编码**：涉及「能不能做」「怎么做」「分析」时，只出方案，禁止写代码、改文件。
2. **大改动前置确认**：涉及架构调整、新功能/模块/接口、跨 3 个及以上文件、数据库变更或重构时，
   须先给方案（目标、修改文件、实现思路、影响范围），收到「确认 / 可以 / go」后再动手。
   复杂任务还须按节点分步暂停确认（表结构、接口设计、核心算法、批量造数/全量测试等）。
   - **每步汇报**：① 本步做了什么 ② 关键结果/方案摘要 ③ 请你确认后再下一步
   - **反例**：
     - 未经确认连续 models+CRUD+router+造数+跑测试
     - 用户只问方向或「能不能做」却直接写完整个功能
     - 用户说「go」但本轮/上一轮未确认过方案仍直接开写
     - 表结构或接口入参/出参未定就写 models/router
     - 一次性写完批量造数、归类、统计接口并执行入库
     - 某步可能推翻前面工作时未说明风险仍继续
3. **小改可直接做**：单文件、范围明确、逻辑清楚的 bug 修复或小改动，可直接修改，不必先出完整方案。
4. **严禁盲目猜测**：信息不足、需求不明、或涉及未知 env/业务逻辑时，须主动提问；不知道就直说。
## 二、代码修改与复用原则
5. **最小修改原则**：只改当前任务直接相关的代码；未经允许不得改动、删除或重构无关文件及历史逻辑。
6. **优先复用代码**：严格优先复用项目已有实现与公共方法，禁止重复造轮子。
7. **红线禁令**：未经允许，严禁新增第三方依赖、严禁修改项目目录结构、严禁改变既有命名风格。
## 三、代码风格与输出规范
8. **保持项目一致性**：编码须遵循项目现有代码风格、命名规范及目录层次。
9. **增量输出**：默认只输出修改部分的 Diff 或核心代码；除非明确要求，禁止整文件贴出。
## 四、答复边界
10. **答复详略分级**：局部问答、语法/笔误 → 极简，就事论事；方案、大改动、多步骤任务 → 按第 2 条结构化展开，不主动扫描整个项目。
11. **默认中文**：与用户沟通、方案说明、注释说明默认使用中文；代码标识符保持项目既有风格。