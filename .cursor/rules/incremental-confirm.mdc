---
description: 分步推进；大改动先确认再写代码；分析请求不动手
alwaysApply: true
---

# 分步确认，避免返工

复杂任务不要一次做完。每完成一个关键节点后暂停，向用户汇报结果并询问是否正确，得到确认后再进入下一步。

## 大改动：先方案，后代码

以下情况**必须先出方案、等用户确认**，确认前**禁止**创建/修改业务代码（含 schema、service、router、crud 等）：

- 新增功能模块或能力（如 Agent 化、报告生成、批量分析）
- 新增或变更 API 接口（路径、入参、出参）
- 新增数据库表或字段
- 跨 3 个以上文件的联动改动
- 引入新架构层（如 AgentService、任务队列）

用户说「go / 开始做」时，若**尚未在本轮或上一轮明确确认过方案**，仍须先补方案摘要并再次确认，不得直接开写。

用户说「先分析 / 能不能做 / 怎么做 / 解释一下」时，**只做分析与方案**，不写代码、不改文件。

## 必须暂停确认的节点

- 整体方案 / 架构设计定稿前
- 数据库表结构、字段含义确定前
- 新增第三方依赖前
- 批量生成测试数据（如 500 条）前
- 核心算法逻辑（如切块策略、归类方式）实现前
- 接口清单与入参/出参设计定稿前
- 跑批量入库或全量测试前

## 每步输出格式

1. **本步做了什么**（或打算做什么）
2. **关键结果 / 方案摘要**（简短）
3. **请你确认**：对不对？确认后我再做下一步

## 禁止行为

- 不要未经确认就连续实现：模型 + CRUD + 路由 + 造数 + 跑测试
- 不要在用户只问了方向时，直接写完整个功能
- 不要在用户要求「先分析」时擅自改代码
- 若某步可能推翻前面工作，必须先说明风险并等待确认

## 示例

```
✅ 正确：先给出表结构和 3 个接口设计 → 等你确认 → 再写 models
✅ 正确：用户问 Agent 化怎么做 → 只讲方案 → 用户确认后再实现
❌ 错误：用户问「能不能做」→ 直接改 schemas + service + router
❌ 错误：一次性写完 500 条造数、归类、统计接口并执行入库
```
