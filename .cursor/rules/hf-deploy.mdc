---
description: Hugging Face Space 推送与部署（禁止二进制、orphan snapshot）
alwaysApply: true
---

# Hugging Face Space 部署约定

远程：`hf` → `https://huggingface.co/spaces/proaugust/cur_index`（Docker，`app_port: 7860`）

## 禁止推送的文件

HF pre-receive hook 会拒绝 git 历史中含大体积二进制。以下文件**不得进入 HF 仓库**（含历史）：

- `vue-manage-system-master/public/models/*.bin`（face-api 权重，**可提交 GitHub**，HF snapshot 由 `deploy-hf.bat` 自动剥离）
- `vue-manage-system-master/screenshots/**`
- 其它未经 [HF Xet](https://huggingface.co/docs/hub/xet) 的大文件（>1MB 二进制）

GitHub `main` 可含人脸模型 `.bin`；**不得**直接推 `main` 到 HF（历史 blob 或 snapshot 含 .bin 会被拒）。部署走 orphan snapshot + 脚本剥离。

## 禁止的操作

- `git push hf main`（直接推 GitHub main，会 non-fast-forward 或触发二进制拒绝）
- 在 HF snapshot 中 `git add` 上述二进制路径
- 为绕过限制而 `git push hf --force` 未净化的 `main`

## 标准部署流程

优先使用仓库根目录 `deploy-hf.bat`（Windows）或等效命令：

1. 确保 `main` 已提交且工作区干净
2. 先 `git push origin main`（GitHub）
3. 创建 **orphan** 分支，提交当前工作区 snapshot（**自动剔除** `.bin` 与 `screenshots`）
4. 提交 snapshot：`HF Space deploy snapshot from main (<short-sha>).`
5. `git push hf hf-deploy-snap:main --force`
6. 切回 `main`，删除临时分支 `hf-deploy-snap`

## 推送前自检

```powershell
# deploy-hf.bat 会从 snapshot 剔除 .bin；手动 push 时勿把 .bin 推上 HF
git diff --cached --name-only | Select-String "\.bin|screenshots"
```

手动推 HF 时若有匹配则中止；用 `deploy-hf.bat` 则脚本会自动剥离后再提交。

## 与 GitHub 的分工

| 目标 | 分支/方式 | 可否含 .bin |
|------|-----------|-------------|
| GitHub `origin` | `main` 正常推送 | **可以**（face-api `.bin` 约 6.5MB） |
| HF Space `hf` | orphan snapshot → `main` | **不可以**（脚本自动剔除） |

考勤人脸打卡在 HF 上不可用属预期（页面会提示）；GitHub 克隆后本地 `npm run dev` 可直接用（`public/models` 含权重，Vite dev 亦会从 `node_modules` 兜底）。

## 部署慢的常见原因

- Cursor 对 `main` 推送需用户批准，未点批准会长时间挂起
- HF Docker 构建：`npm ci` + `vite build`、`pip install` torch/sentence-transformers，通常比 git push 更久
- 勿将「构建排队」误判为「推送失败」；到 Space **Logs** 查看构建状态
